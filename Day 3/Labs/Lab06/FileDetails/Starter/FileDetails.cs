using System;                // Console
using System.IO;             // File, StreamReader

namespace Lab06
{
    class FileDetails
    {
        static void Main(string[] args)
        {
            // Get file name from command line

            // If the file exists, create a StreamReader 
            // object to open the file for reading

            // Determine the length of the file
            // Create a char array large enough to hold
            // file's contents

            // Read the file into the char array

            // Pass the array to the Summarize method
            // (defined below)
        }

        // Create a static method named Summarize that 
        // accepts an array of char and returns void

        // Loop through the array, counting how many
        // vowels, consonants, and lines it contains

        // Print the total number of characters, vowels,
        // consonants, and lines to the console
    }
}