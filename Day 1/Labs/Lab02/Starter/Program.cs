using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Lab02
{

    // Create an enumeration named AccountType
    // defining the following account types: 
    // Checking, Savings, CD, and Loan.

    // Create a structure named BankAccount 
    // with a string field named Number, 
    // an AccountType field named Type, and 
    // a decimal field named Balance.

    class Program
    {
        static void Main(string[] args)
        {
            // Define a variable of type BankAccount
            // named account.

            // Prompt the user to enter an account number.
            // Store the value in the account variable's 
            // Number field.

            // Prompt the user to enter an account type.
            // Store the value in the account variable's 
            // Type field.

            // Prompt the user to enter an opening balance.
            // Store the value in the account variable's 
            // Balance field.

            // Display the Number, Type, and Balance values
            // of the account.
        }
    }
}
